using System;

namespace Fundamentals_1
{
    class Program
    {
        static void Main(string[] args)
        {
            string place = "Coding Dojo";
            Console.WriteLine($"You are attending {place}");
            System.Console.WriteLine("The value of pi is " + 3.14159);
            // Create a Loop that prints all values from 1-255
            // Fundaments.loop255();
            //  Create a new loop that prints all values from 1-100 that are divisible by 3 or 5, but not both
            // Fundaments.loop3n5();
            //  Modify the previous loop to print "Fizz" for multiples of 3, "Buzz" for multiples of 5, and "FizzBuzz" for numbers that are multiples of both 3 and 5
            //Fundaments.fizzBuzz();
            //  (Optional) If you used "for" loops for your solution, try doing the same with "while" loops. Vice-versa if you used "while" loops!
            
        }
    }
}
